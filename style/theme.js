import React from "react";
import { DefaultTheme } from "react-native-paper";

export const theme = {
    ...DefaultTheme,
    colors: {
      ...DefaultTheme.colors,
      primary: "#1B998B",
      accent: '#656176',
      text: '#534D56'
    }
  }